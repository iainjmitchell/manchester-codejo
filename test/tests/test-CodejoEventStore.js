(function($, undefined){
	module('CodejoEventStore - Integration Tests against ACL');

	// asyncTest('Notified of codejo events when codejo event store refreshed', function(){
	// 	expect(1);
	// 	var  notifyCodejoEvents = function(codejoEvents){
	// 			ok(codejoEvents.length >= 0);
	// 			start();
	// 		},
	// 		target = new CodejoEventStore(notifyCodejoEvents);
	// 	target.refresh();
	// });
})(jQuery);

